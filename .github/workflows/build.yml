




      - name: Build Loop App
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          echo "Checking directory structure..."
          pwd
          ls -la
          ls -la Loop || true
